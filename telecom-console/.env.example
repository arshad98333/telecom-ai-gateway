# Console dev server (Vite). Proxies /api and /mcp to local backends.
# VITE_MIDDLEWARE_BASE_URL=
# VITE_MCP_BASE_URL=
# VITE_API_PREFIX=/api/v1
# Optional: paste a token from `make token` to skip manual entry
# VITE_DEV_TOKEN=
